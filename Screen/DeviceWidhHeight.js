import { useWindowDimensions } from "react-native";

export const deviceWidth = useWindowDimensions().width
export const deviceHeigh = useWindowDimensions().height
export const deviceScale = useWindowDimensions().scale
export const deviceFontScale = useWindowDimensions().fontScale